import React from "react";

const Layout = ({ children }) => {
    return (
        <div>
            <h2>teasie?</h2>
            <div>{children}</div>
        </div>
    );
}

export default Layout;